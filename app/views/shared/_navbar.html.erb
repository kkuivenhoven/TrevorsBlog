<nav class="navbar navbar-expand-lg navbar-dark bg-dark w-100">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Trevor's Blog</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to "About", pages_about_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Blog Posts", blog_posts_path, class: "nav-link" %>
        </li>
		<% if current_user && current_user.admin? %>
			<li class="nav-item">
			  <%= link_to "New Post", posts_new_upload_path, class: "nav-link" %>
			</li>
		<% end %>
		<% if current_user && current_user.admin? %>
			<li class="nav-item">
			  <%= link_to "New Decision Tree", decision_tree_new_upload_path, class: "nav-link" %>
			</li>
		<% end %>

		<li class="nav-item">
		  <%= link_to "Fraud Simulators", decision_tree_index_path, class: "nav-link" %>
		</li>

			<% if user_signed_in? %>
				<% unless Rails.env.production? %>
					<li class="nav-item">
					  <%= link_to "Fraud Prompts", fraud_prompts_path, class: "nav-link" %>
					</li>
				<% end %>

				<%= link_to "Email Preferences", edit_user_path(current_user), class: "nav-link" %>
				<% unless Rails.env.production? %>
					<%= link_to "Edit Profile", edit_user_registration_path, class: "nav-link" %>
				<% end %>
				<%= link_to "Sign Out", destroy_user_session_path, data: { "turbo-method": :delete }, class: "nav-link" %>

				<% if current_user && current_user.admin? %>
				  <%= link_to "Admin Dashboard", admin_dashboard_path, class: "nav-link" %>
				<% end %>
			<% else %>
				<%= link_to 'Login', new_user_session_path, class: "nav-link" %>
				<%= link_to 'Sign up', new_user_registration_path, class: "nav-link" %>
			<% end %>
		  </ul>

    </div>
  </div>
</nav>

